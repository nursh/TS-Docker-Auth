version: '2.4'

services:
  server:
    image: server
    build:
      context: ./server
    ports:
      - "8080:8080"
    volumes:
      - /usr/src/app/node_modules
      - ./server:/app
      - /usr/src/app/coverage
