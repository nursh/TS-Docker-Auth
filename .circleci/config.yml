version: 2.1

orbs: 
  node: circleci/node@3.0.0

jobs:
  server:
    executor: node/default

    steps:
      - checkout
      - node/install-packages:
          app-dir: ~/project/server
          run-command: test

workflows:
    build-and-test:
      jobs:
        - server